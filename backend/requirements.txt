# Core
django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0

# Database
psycopg[binary]==3.2.3

# Redis + Celery (scheduled emails)
redis==5.2.1
celery==5.4.0
django-celery-beat==2.7.0

# Payments
paddle-python-sdk==1.3.0

# Email
resend==2.5.1

# Media uploads — MinIO via S3-compatible API (self-hosted)
boto3==1.35.93
django-storages[s3]==1.14.4

# Environment
python-decouple==3.8

# Production server
gunicorn==23.0.0

# Dev / linting
ruff==0.9.1
